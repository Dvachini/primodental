{
  "name": "primodental",
  "scripts": {
    "install-client": "cd client && bun install",
    "install-server": "bun install",
    "client": "cd client && bun run dev",
    "dev-server": "bun --env-file .env --watch server.js",
    "dev": "conc -n \"CLIENT,API\" -c \"cyan,magenta,yellow\" \"npm:client\" \"npm:dev-server\"",
    "build-client": "cd client && bun run build",
    "start": "NODE_ENV=production bun --env-file .env server.js"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "compression": "^1.8.1",
    "concurrently": "^9.2.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "module": "server.js",
  "type": "module",
  "private": true
}
